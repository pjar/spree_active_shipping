<% content_for :page_actions do %>
  <li><%= link_to_with_icon('icon-plus', Spree.t(:new_package), new_admin_order_order_package_url(@order), :id => 'new_order_package_link', :class => 'button') %></li>
<% end %>

<% content_for :head do %>
  <%= javascript_include_tag 'admin/order_packages/index.js' %>
<% end %>

<%= render :partial => 'spree/admin/shared/order_tabs', :locals => { :current => 'Order Packages' } %>

<div id="order_packages" data-hook></div>

<% unless @order.reload.order_packages.any? %>
  <div class="no-objects-found">
    <%= Spree.t(:no_packages_found) %>.
  </div>
<% else %>
  <table class="index">
    <thead data-hook="option_header">
      <tr data-hook="order_packages_header">
        <th><%= Spree.t(:length) %></th>
        <th><%= Spree.t(:width) %></th>
        <th><%= Spree.t(:height) %></th>
        <th><%= Spree.t(:weight) %></th>
        <th class="actions"></th>
      </tr>
    </thead>
    <tbody id="order_packages" data-hook>
      <% @order.order_packages.each do |package| %>
        <tr id="<%= dom_id(package) %>" data-hook="order_package_row">
          <td><%= package.length %></td>
          <td><%= package.width %></td>
          <td><%= package.height %></td>
          <td><%= package.weight %></td>
          <td class="actions">
            <%= link_to_with_icon 'icon-edit', Spree.t(:edit), edit_admin_order_order_package_url(@order, package), :class => "edit", :no_text => true, :data => {:action => 'edit'} %>
            &nbsp;
            <%= link_to_delete package, { :url => admin_order_order_package_url(@order, package), :no_text => true }%>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
